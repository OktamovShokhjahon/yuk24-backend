const User = require('./User');
const Driver = require('./Driver');
const Admin = require('./Admin');
const Order = require('./Order');
const Review = require('./Review');

module.exports = { User, Driver, Admin, Order, Review };
