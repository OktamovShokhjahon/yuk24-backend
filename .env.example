# Server
PORT=5000

# MongoDB
MONGODB_URI=mongodb://localhost:27017/yuk24

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRY=7d

# Admin (MVP: plain password; hash in production)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Optional: OpenRouteService for route/distance (POST /api/route)
# ORS_API_KEY=

# Optional: frontend origin for CORS
# VITE_APP_URL=http://localhost:5173
